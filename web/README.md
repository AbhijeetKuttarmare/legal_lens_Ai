# LegalLens AI — Web

Placeholder for a future web client. Not part of the initial mobile-first MVP build.

When this gets built out, it will talk to the same NestJS backend in [`../backend`](../backend) — reuse the API contracts documented there (auth, documents, chat) rather than duplicating logic.
